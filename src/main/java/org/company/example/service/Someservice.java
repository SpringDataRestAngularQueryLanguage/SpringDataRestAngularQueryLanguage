package org.company.example.service;

/*
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("/someservice")
public class Someservice {

    @RequestMapping(value = "/{findall}", method = RequestMethod.GET)
    @ResponseBody List<Parent> findByName(String some){
        ParentService parentService = ApplicationMain.context.getBean(ParentService.class);
        return parentService.findAll();
    }

    @RequestMapping(value = "/feedthekid", params = {"id"}, method = RequestMethod.POST)
    public @ResponseBody String runTest(@RequestParam(value = "id") long id) {

        ParentService parentService = ApplicationMain.context.getBean(ParentService.class);
        Parent parent = parentService.findOne(id);

        return parent.feedTheKid();
    }
}*/
